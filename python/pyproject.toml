[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[project]
name = "lyricfetch"
version = "0.1.0"
description = "Standard-library Python wrapper for the lyricfetch Go CLI"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
